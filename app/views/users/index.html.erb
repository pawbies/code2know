<h1>All Users</h1>

<% @users.each do |user| %>

<h2><%= link_to user.username, user_path(id: user.id) %></h2>

<p>
  Rank: <%= user.rank.name %><br>
  Level: <%= user.role.name %><br>
  XP: <%= user.xp %>
</p>

<hr>

<% end %>
